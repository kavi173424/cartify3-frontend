import { fakeAsync } from "@angular/core/testing";


export const environment = {
  production: false,
  apiBase: 'https://localhost:7113/api', // your API base
  apiHost: 'https://localhost:7113'       // for images (no /api!)
};